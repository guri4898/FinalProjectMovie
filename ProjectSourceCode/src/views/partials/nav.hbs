<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    .navbar {
      background-color: #11091c;
    }
    .dropdown {
      position: relative;
      display: inline-block;
    }
    /*Button color*/
    .dropbtn {
      background-color: #be3265;
      color: white;
      padding: 16px;
      font-size: 16px;
      border: none;
      cursor: pointer;
      border-radius: 12px;
    }
    /*This hides the list so that it only appears when its a drop down*/
    .dropdown-content {
      display: none;
      position: absolute;
      background-color: #f9f9f9;
      min-width: 160px;
      box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
      z-index: 1;
    }
    /*Color and settings of the drop down boxes*/
    .dropdown-content a {
      background-color: white;
      color: #7f2244;
      padding: 12px 16px;
      text-decoration: none;
      display: block;
    }
    /*Allows for the buttons to come down when the user hovers over either of the buttons*/
   .dropdown:hover .dropdown-content, 
.dropdown-content:hover {
  display: block;
}

    /*Need to figure out what we want the website design to look like*/
    /**/
  </style>
</head>
<body>
{{#if display}}
<header>
  <nav class="navbar navbar-expand-sm border-bottom">
    <div class="container">
      <!-- This is for the drop down buttons on the left side -->
      <div class="dropdown">
        <button class="dropbtn">Options</button>
        <div class="dropdown-content">
          <!-- Options for the user to select-->
          <a href="/home">Home Page</a>
          <a href="/addFriend">Add Friend</a>
          <a href="/addMovie">Add Movie</a>
          <a href="/favoriteMovies">Favorite Movies</a>
        </div>
      </div>

      <!-- This is for the right side of the website -->
      <div class="dropdown right">
        <button class="dropbtn">Account</button>
        <div class="dropdown-content">
          <!-- Options for the user to select -->
          <!-- Not sure why it's not mapping to the account settings -->
          <a href="/account_settings">Account Settings</a>
          <a href="/logout">Logout</a>
        </div>
      </div>
    </div>
  </nav>
</header>
{{/if}}
</body>
</html>
